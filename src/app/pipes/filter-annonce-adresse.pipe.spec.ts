import { FilterAnnonceAdressePipe } from './filter-annonce-adresse.pipe';

describe('FilterAnnoncePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAnnonceAdressePipe();
    expect(pipe).toBeTruthy();
  });
});
