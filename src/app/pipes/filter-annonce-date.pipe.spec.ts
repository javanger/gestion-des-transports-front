import { FilterAnnonceDatePipe } from './filter-annonce-date.pipe';

describe('FilterAnnonceDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAnnonceDatePipe();
    expect(pipe).toBeTruthy();
  });
});
